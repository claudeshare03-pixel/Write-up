I created a program that uses the MQTT protocol to quickly share data between multiple devices.

Although it includes all the essential features of MQTT, I built a testbed using only simple functions.
